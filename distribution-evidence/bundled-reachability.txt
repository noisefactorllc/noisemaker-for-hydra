# Reachability of the 16 production advisories (npm audit --omit=dev)
# Tree: isolated build at the implementation commit (see commands.txt).
#
# Bundling evidence: esbuild metafile input listing for the production entry
# (index.js, bundle=true, platform=browser), mirroring the vite production
# build's dependency resolution. The full list of 82 bundled node_modules
# packages with installed versions is retained in bundled-packages.txt, and
# per-package LICENSE copies (or, where the installed package ships none, the
# package's own license statement) are retained in third-party-licenses/.
#
# Advisory module -> bundled? (esbuild metafile) -> reachable in default artifact?
#
# i18next-http-backend (moderate): NOT bundled (absent from metafile; import
#   commented out at src/stores/language-store.js:3). Not reachable.
# form-data (critical) via superagent: NOT bundled as a module (absent from
#   metafile). The single 'form-data' string in dist/assets/index-aecb58b8.js
#   is superagent's MIME-type constant ('form-data':'application/x-www-form-urlencoded');
#   form-data's own dependencies hexoid and combined-stream are absent from the
#   metafile. Not reachable.
# qs (moderate) via superagent: BUNDLED (esbuild metafile; 'arrayLimit' present
#   in dist/assets/index-aecb58b8.js). Dormant: the superagent request calls in
#   src/stores/gallery.js execute only when state.serverURL is set from
#   VITE_SERVER_URL (src/stores/store.js:13); the distributed build defines no
#   VITE_SERVER_URL, so serverURL is null and these paths are unreachable.
# socket.io-client / engine.io-client / socket.io-parser / parseuri (high/
#   moderate): BUNDLED (all four present in the esbuild metafile for the
#   patch-bay import chain). Note: the plain-text grep of the minified bundle
#   shows 0 hits for 'parseuri' because the minifier renamed it; the metafile
#   is the authoritative bundling evidence. Dormant: PatchBay is constructed
#   only under the same null serverURL guard (src/views/Hydra.js:32).
# shortid (moderate, via nanoid): BUNDLED (metafile; shortid and nanoid both
#   listed). Dormant: only executed inside patch-bay peer-id generation.
# ws (high): NOT bundled (server-side; absent from metafile). Not reachable.
# formidable (low): NOT bundled (absent from metafile). Not reachable.
# min-document (low): NOT bundled as a module (absent from metafile; choo's
#   'global' shim is bundled but min-document itself is not). Not reachable.
# minimatch / brace-expansion (high): NOT bundled (js-beautify Node CLI
#   dependency chain; absent from metafile). Not reachable.
# semver (high): NOT bundled (absent from metafile). Not reachable.
# @babel/runtime (moderate, via i18next): BUNDLED (metafile lists
#   @babel/runtime 7.22.5). The advisory's vulnerable RegExp path applies to
#   generated .replace() code paths not exercised by the bundled i18next
#   usage; the artifact is a static demo page with no attacker-controlled
#   regular-expression input. Assessed: no known reachable path in this
#   artifact; recorded, not eliminated.
#
# Decision: no dependency upgrade in this round (shortid->nanoid and
# socket.io-client 2->4 change public APIs used by the vendored patch-bay
# code and would alter behavior without rendered qualification). Advisories
# are assessed with bundling evidence above; dormant bundled chains are
# re-assessable if a server-integrated build (VITE_SERVER_URL set) is produced.
